<!DOCTYPE html>

<html>
    <header>
        <title>404</title>
        <link rel="icon" href="/img/weather.png">
        <link rel="stylesheet" href="/css/styles.css">
    </header>

    <body>
        <div class="main-content">
            {{>header}}
            <h4>{{errorMessage}}</h4>
        </div>

        {{>footer}}
    </body>
</html>